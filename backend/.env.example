# Backend environment variables (copy to .env for local runs WITHOUT docker).
# When running via docker-compose these are supplied by the compose file / root .env.

DATABASE_URL=postgresql+psycopg2://inventory_user:change_me@localhost:5432/inventory_db
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
LOW_STOCK_THRESHOLD=10
