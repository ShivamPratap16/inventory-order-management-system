# Frontend environment variables.
# Vite only exposes vars prefixed with VITE_ to the browser bundle.
# Point this at the backend API. Locally with docker-compose it is the host
# port mapped to the backend; in production it is your deployed backend URL.
VITE_API_BASE_URL=http://localhost:8000
